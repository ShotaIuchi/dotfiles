;; ========================================================================
;;  grep
;; ========================================================================
(setq null-device "/dev/null")
(global-set-key (kbd "C-M-g") 'rgrep)


;; ========================================================================
;;  ag
;; ========================================================================
(when 'flag-helm-init
  (if (not (require 'helm-ag nil t))
      (message "!!! WORNING !!! | require : helm-ag")
    (global-set-key (kbd "C-u C-g") 'helm-ag)))


;; ========================================================================
;;  edit
;; ========================================================================
(unless (require 'wgrep nil t)
  (message "!!! WORNING !!! | require : wgrep"))


(provide 'sub-search-grep)
