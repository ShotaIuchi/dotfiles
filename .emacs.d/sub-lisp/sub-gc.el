;; ========================================================================
;;  garbage collect
;; ========================================================================
(setq gc-cons-threshold 100000000)
(setq garbage-collection-messages "run-GC")


(provide 'sub-gc)
