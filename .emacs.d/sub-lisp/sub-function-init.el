;; ========================================================================
;;  reload
;; ========================================================================
(defun reload ()
  (interactive)
  (load-file "~/.emacs.d/init.el"))


(provide 'sub-function-init)
