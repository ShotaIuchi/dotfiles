;; ========================================================================
;;  file
;; ========================================================================
(setq recentf-max-menu-items 50)
(setq recentf-max-saved-items 3000)

;; ========================================================================
;;  mini buffer
;; ========================================================================
(savehist-mode 1)
(setq history-length 3000)


(provide 'sub-recent)
